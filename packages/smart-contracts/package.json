{
  "name": "@jeton/smart-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "aptos move compile --move-2 --named-addresses zk_deck=default,jeton=default",
    "test": "aptos move test --move-2 --named-addresses zk_deck=default,jeton=default"
  },
  "devDependencies": {
    "aptos": "^1.21.0"
  }
}
