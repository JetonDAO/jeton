export const runtime = "edge";
import Modal from "@jeton/ui/Modal";

export default function LoginModal() {
  return (
    <Modal>
      <div className="text-center mt-[50vh]">
        this is login page but opened as modal
      </div>
    </Modal>
  );
}
